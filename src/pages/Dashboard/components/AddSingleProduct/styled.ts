import styled from '@emotion/styled'
import { Box } from '@mui/material'

export const InputRow = styled(Box)(() => ({
   display: 'flex',
   gap: '30px',
   marginBottom: '30px',
   alignItems: 'start',
}))
